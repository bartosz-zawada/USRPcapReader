Useless Small Ruby Pcap Reader

Reads a Pcap file passed through command line and prints unix time and size of each packet in the file
The default output format is JSON, but may be changed to XML if the script is called with the --xml parameter


Requires ruby-pcap:
https://github.com/ahobson/ruby-pcap
ruby-pcap is copyrighted free software by Masaki Fukushima.
